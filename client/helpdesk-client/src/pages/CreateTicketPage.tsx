import{useNavigate}from'react-router-dom';import{createTicketAsync}from'../api/tickets';import{TicketForm}from'../components/TicketForm';export function CreateTicketPage(){const navigate=useNavigate();return<section><h1>Create ticket</h1><TicketForm cancelTo="/app/tickets" onSubmit={async request=>{const ticket=await createTicketAsync(request);navigate(`/app/tickets/${ticket.id}`)}}/></section>}
