import type {TicketListRequest,TicketSortField,SortDirection} from '../types/tickets'
export const ticketSortFields:TicketSortField[]=['CreatedAtUtc','UpdatedAtUtc','TicketNumber','Priority','Status','Title']
export function parseTicketQuery(q:URLSearchParams):TicketListRequest{const number=(key:string,fallback:number,max=Infinity)=>{const n=Number(q.get(key));return Number.isInteger(n)&&n>0&&n<=max?n:fallback};const sort=q.get('sortBy');const direction=q.get('sortDirection');return{pageNumber:number('page',1),pageSize:number('pageSize',20,100),search:q.get('search')||undefined,categoryId:q.get('category')?number('category',0):undefined,priorityId:q.get('priority')?number('priority',0):undefined,statusId:q.get('status')?number('status',0):undefined,createdFromUtc:q.get('from')||undefined,createdToUtc:q.get('to')||undefined,sortBy:ticketSortFields.includes(sort as TicketSortField)?sort as TicketSortField:'CreatedAtUtc',sortDirection:direction==='asc'||direction==='desc'?direction as SortDirection:'desc'}}
export function formatDate(value:string|null|undefined){if(!value)return '—';const date=new Date(value);return Number.isNaN(date.getTime())?'Invalid date':new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(date)}
export const isGuid=(value:string|undefined)=>!!value&&/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value)
