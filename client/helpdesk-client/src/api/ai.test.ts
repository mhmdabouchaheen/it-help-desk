import{beforeEach,describe,expect,it,vi}from'vitest';import{analyzeTicketAsync}from'./ai';import{tokenStore}from'../auth/tokenStore'
describe('AI API',()=>{beforeEach(()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({summary:'s'}),{status:200,headers:{'content-type':'application/json'}})));tokenStore.clear()});it('posts only to the encoded ticket route and forwards AbortSignal',async()=>{tokenStore.set({accessToken:'secret-token',refreshToken:'refresh'});const signal=new AbortController().signal;await analyzeTicketAsync('ticket/id',signal);const[url,options]=vi.mocked(fetch).mock.calls[0];expect(url).toBe('https://api.test/api/tickets/ticket%2Fid/ai-analysis');expect(String(url)).not.toContain('secret-token');expect(options?.method).toBe('POST');expect(options?.body).toBeUndefined();expect(options?.signal).toBe(signal)})})
